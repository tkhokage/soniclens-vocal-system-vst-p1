# Voice Analyzer — Dev setup

Quick dev environment for the prototype.

## Setup

Install dependencies:

```bash
npm install
```

Run dev server:

```bash
npm run dev
```

Format and lint:

```bash
npm run format
npm run lint
```

Notes:
- Project uses Vite + React + TypeScript.
- If you want, I can run `npm install` here for you (ask me to).
